# Per-user git identity generated by `agent` from host git config.
git-local.nix

# Per-user network params generated by `agent` from .env. See network.nix.
network-local.nix

# Per-user env overrides, seeded from .env.defaults on first run.
.env

.claude/
